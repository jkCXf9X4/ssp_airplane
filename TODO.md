
Notes:
[] Check out - Another F-16 simulation? A  user-friendly Julia, MATLAB and Simulink flight simulation 
 Duc Nguyen, Mark Lowenberg

 TODO:
 * [x] Make C build script more generic so it can handle a generic C/C++ build
 * [x] Move generic `fmi2*.h` headers to `3rd_party/fmi_headers`
 * [ ] Check so it can fly from parameters
   * Clarification needed: does this mean SSP parameter binding only, scenario waypoint injection, or full aircraft initialization from an SSV/JSON parameter set?
 * [x] Fix documentation for initial setup
   * [x] Include common build dependencies and instructions for C++ and `omc`
 
